import React from "react";

const Comp = ({ data }) => {
  return (
      <div>
        {/* {data.map((item) => (
          <p key={item.id}>{item.name}</p>
        ))} */}
      </div>
  );
};

export default Comp;
